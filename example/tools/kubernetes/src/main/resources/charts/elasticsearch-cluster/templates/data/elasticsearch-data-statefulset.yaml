{{- if .Values.elasticsearch_data.enable }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: elasticsearch-data-statefulset
  labels:
    app: "elasticsearch"
    release: "{{ .Release.Name }}"
spec:
  serviceName: "elasticsearch-data-headless-service"
  replicas: {{ .Values.elasticsearch_data.replicas | default 1 }}
  selector:
    matchLabels:
      app: "elasticsearch"
      release: "{{ .Release.Name }}"
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: "elasticsearch"
        release: "{{ .Release.Name }}"
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      hostNetwork: true
      restartPolicy: Always
      {{- if .Values.elasticsearch_data.affinity }}
      affinity:
      {{ tpl .Values.elasticsearch_data.affinity . | indent 8 }}
      {{- end }}
      containers:
      - name: elasticsearch-data
        image: "{{ .Values.image }}:{{ .Values.imageTag }}"
        imagePullPolicy: IfNotPresent
        command: ['/bin/bash', '-c', '/bin/bash /run.sh']
        env:
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
        livenessProbe:
          tcpSocket:
            port: {{.Values.http_port}}
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 60
          failureThreshold: 8
          successThreshold: 1
        volumeMounts:
          - name: ro
            mountPath: /etc/localtime:ro
          - name: lib64
            mountPath: /usr/lib64
          - name: data
            mountPath: /elasticsearch/data
          - name: elasticsearch-data-configmap
            mountPath: /run.sh
            subPath: run.sh
          - name: elasticsearch-data-configmap
            mountPath: /elasticsearch/config/elasticsearch.yml.bak
            subPath: elasticsearch.yml.bak
      volumes:
        - name: ro
          hostPath:
            path: /etc/localtime
        - name: lib64
          hostPath:
            path: /usr/lib64
        - name: elasticsearch-data-configmap
          configMap:
            name: elasticsearch-data-configmap
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 10Gi
      storageClassName: "local-path"
{{- end }}