{{- if .Values.elasticsearch_ingest.enable }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: elasticsearch-ingest-statefulset
  labels:
    app: "elasticsearch"
    release: "{{ .Release.Name }}"
spec:
  serviceName: "elasticsearch-ingest-headless-service"
  replicas: {{ .Values.elasticsearch_ingest.replicas | default 1 }}
  selector:
    matchLabels:
      app: "elasticsearch"
      release: "{{ .Release.Name }}"
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: "elasticsearch"
        release: "{{ .Release.Name }}"
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      hostNetwork: true
      restartPolicy: Always
      {{- if .Values.elasticsearch_ingest.affinity }}
      affinity:
      {{ tpl .Values.elasticsearch_ingest.affinity . | indent 8 }}
      {{- end }}
      containers:
      - name: elasticsearch-ingest
        image: "{{ .Values.image }}:{{ .Values.imageTag }}"
        imagePullPolicy: IfNotPresent
        command: ['/bin/bash', '-c', '/bin/bash /run.sh']
        env:
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
        livenessProbe:
          tcpSocket:
            port: {{.Values.http_port}}
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 60
          failureThreshold: 8
          successThreshold: 1
        volumeMounts:
          - name: ro
            mountPath: /etc/localtime:ro
          - name: lib64
            mountPath: /usr/lib64
          - name: data
            mountPath: /elasticsearch/data
          - name: elasticsearch-ingest-configmap
            mountPath: /run.sh
            subPath: run.sh
          - name: elasticsearch-ingest-configmap
            mountPath: /elasticsearch/config/elasticsearch.yml.bak
            subPath: elasticsearch.yml.bak
      volumes:
        - name: ro
          hostPath:
            path: /etc/localtime
        - name: lib64
          hostPath:
            path: /usr/lib64
        - name: elasticsearch-ingest-configmap
          configMap:
            name: elasticsearch-ingest-configmap
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 10Gi
      storageClassName: "local-path"
{{- end }}